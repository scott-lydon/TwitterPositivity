# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterPositivity' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterPositivity

  target 'TwitterPositivityTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TwitterPositivityUITests' do
    inherit! :search_paths
    # Pods for testing
  end

    pod 'Fabric'
    pod 'TwitterKit'
    pod 'Firebase/Auth'

end
